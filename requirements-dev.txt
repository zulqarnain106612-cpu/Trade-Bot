pytest>=8
pytest-asyncio>=0.23
pytest-cov>=5
ruff==0.4.4  # Debt-006: pinned to match .trunk/trunk.yaml, .pre-commit-config.yaml, and auto-fix.yml -- previously >=0.0.332 let `pip install` drift to whatever's latest (confirmed 0.15.17 in one audited .venv), silently diverging from every other pinned config.
black>=24.10.0
isort>=5.13.0
detect-secrets>=1.4
pip-tools>=7.4
# watchdog removed — never imported in src/ (was dead dependency in both req files)
