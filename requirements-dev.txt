pytest>=8
pytest-asyncio>=0.23
pytest-cov>=5
ruff==0.15.21  # Debt-006: pinned to match .trunk/trunk.yaml, .pre-commit-config.yaml, and auto-fix.yml -- previously >=0.0.332 let `pip install` drift to whatever's latest (confirmed 0.15.17 in one audited .venv), silently diverging from every other pinned config.
mypy>=1.11  # required by CLAUDE.md validation gate (uv run mypy src/) — was referenced but never declared
black>=24.10.0
isort>=5.13.0
detect-secrets>=1.4
pip-tools>=7.5.3
# watchdog removed — never imported in src/ (was dead dependency in both req files)
