# Core runtime dependencies.
#
# Regenerated from the imports actually present in src/, kg/, rag_mongo/,
# orchestrator/ and common/ (see the config-purge follow-up). Heavy ML extras
# live in requirements-optional.txt; test and lint tooling in
# requirements-dev.txt. Target interpreter: Python 3.11 (.python-version).

# --- async I/O and HTTP -----------------------------------------------------
aiohttp>=3.9,<4.0
httpx>=0.27,<1.0
requests>=2.32,<3.0
websockets>=12.0,<16.0

# --- API surface ------------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# --- storage ----------------------------------------------------------------
aiosqlite>=0.20,<1.0
asyncpg>=0.29,<1.0
duckdb>=1.0,<2.0
pymongo>=4.9,<5.0

# --- exchange / market data -------------------------------------------------
ccxt>=4.3,<5.0

# --- numerics and modelling -------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
scikit-learn>=1.9.0,<2.0
statsmodels>=0.14,<1.0
networkx>=3.3,<4.0
joblib>=1.4,<2.0

# --- ops --------------------------------------------------------------------
cryptography>=50.0.0,<51.0
prometheus-client>=0.20,<1.0
structlog>=24.1,<26.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0

# --- hard imports at module scope -------------------------------------------
# These are NOT optional despite being heavy: each is imported unguarded at
# module level, so the importing module fails outright without it.
#   xgboost               src/engine/signal_engine.py
#   hmmlearn              src/regime/detector.py, depth_detector_v2.py
#   optuna                src/tuning/bayesian_proposer.py
#   sentence-transformers rag_mongo/embeddings.py
#   torch                 src/fusion/*, src/models/bert_head.py -- and
#                         tests/test_meta_network_loss.py imports it at module
#                         scope, so pytest needs it at collection time.
xgboost>=2.0,<4.0
hmmlearn>=0.3,<1.0
optuna>=3.6,<5.0
sentence-transformers>=3.0,<6.0
torch>=2.3,<3.0
