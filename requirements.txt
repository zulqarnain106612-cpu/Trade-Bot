# Version-range mirror of requirements.in, for the one consumer that cannot use
# the lock: mutation-testing.yml runs on a Python older than the lock's
# numpy==2.5.0 supports, so it installs ranges instead of hashes.
#
# INVARIANT: every package here must also be in requirements.in.
# CI installs requirements.lock, which pip-compile builds from requirements.in
# ONLY — a package declared here and nowhere else is never installed anywhere.
# Enforced by scripts/check_requirements_sync.py.
#
# Optional, lazily-imported extras are NOT here; see requirements-optional.txt.
aiohttp==3.14.3
aiosqlite==0.22.1
asyncpg==0.31.0
ccxt==4.5.71
duckdb==1.5.5
fastapi==0.141.1
hmmlearn==0.3.3
httpx==0.28.1
joblib==1.5.3
numpy==2.4.6
optuna==4.9.0
pandas==2.3.3
prometheus-client==0.26.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2
rich==14.3.4
river==0.25.0
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.14.6
structlog==25.5.0
torch==2.13.0
uvicorn[standard]==0.52.1
websockets==17.0.1
xgboost==2.1.4
