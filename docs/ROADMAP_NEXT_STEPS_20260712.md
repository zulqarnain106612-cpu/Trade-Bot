# Trade-Bot-main — Verified State & Roadmap (2026-07-12)
Generated by direct filesystem/git inspection. Not derived from CLAUDE.md/GAPS.md claims.

## 0. CRITICAL — Concurrent Session Conflict
`.coverage` file mutated (167936→221184 bytes) *during* this analysis with no
pytest process visible in `ps aux`. A Claude Code / terminal session is
actively writing to this repo right now. **Do not run pytest, coverage, or
git commit from a second session concurrently** — SQLite coverage db and
git index will race/corrupt. Confirm the other session is idle before any
write action below.

## 1. Verified Current State
- Branch `main`, HEAD `e22d12a`. **17 tracked files modified, uncommitted**
  (mid-flight: self-tuning Phase 8, `src/config.py`, `src/tuning/*`,
  `src/regime/detector.py`, `src/risk/slippage.py`, `scripts/run_tuning_attempt.py`).
- **7 untracked paths**: `.claude/agents/`, `.claude/hooks/`,
  `src/tuning/live_overrides.py`, `.github/workflows/claude-code-review.yml`,
  + 5x `.claude/settings.json.bak.*` (stale debug artifacts — delete or gitignore).
- `coverage report` returns **"No data to report"** on the committed `.coverage`
  file when read independently — cannot confirm the 95% gate claim right now.
  Last *recorded* value in `.project-intel/SESSION_STATE.json` is
  **coverage_pct: 62**, timestamp 2026-07-12T12:32:42. Treat "95% achieved" as
  **unverified** until the in-progress session finishes and reports its own
  `coverage report` output.
- Global gate is real: `pyproject.toml` → `--cov-fail-under=95` in pytest
  addopts. Per-file floors enforced separately by
  `scripts/check_coverage_floors.py` (not the global number) — floors:
  `src/execution/ ≥75%`, `src/engine/ ≥60%`, `runtime_monitor ≥50%`.
- Open item in session state: **Debt-003** — `.venv` runs Python 3.14.4 but
  `pyproject.toml`/pyright/mypy pin **3.11**. Never validated against pinned
  version. This is a real reproducibility risk, not resolved by coverage %.
- Open item: **GAP-006** — storage backend undecided (TimescaleDB vs QuestDB).

## 2. Configuration That Can Interrupt/Block Claude
- `.claude/settings.json` (working copy, uncommitted, +93/-19 vs HEAD):
  - `deny` list blocks: `.env*` read/write, `/data`, `/logs`, `/models`,
    `.venv` reads, `pip *`, force-push, `TRADING_MODE=live*`,
    `execution-mode AUTOMATIC/RESTRICTED` toggles, deleting
    `src/execution/*` or `src/risk/*`. → Any task touching these will be
    **auto-denied**, not just slow.
  - `ask` list requires manual confirmation to edit: hooks, settings*.json,
    `.github/workflows/**`. → Self-modifying the permission system pauses
    for approval — expected, not a bug.
  - `PostToolUse` hook (`log-edit.sh`) logs every Edit/Write/Bash touching
    src/tests/scripts to `.claude/review-queue.log`.
  - `Stop` hook (`stop-review.sh`) **blocks session end** (exit 2) if the
    queue is non-empty, forcing a `code-reviewer` subagent run before Claude
    can finish. Expect this to fire on any coding session — not an error.
- `.claude/settings.local.json`: `"defaultMode": "dontAsk"` — **local override
  bypasses the `ask` gate above** for anything matching its own allow list.
  This file also still contains ~20 narrowly-scoped debug commands from a
  past coverage-boosting session (e.g. `/tmp/tb-py311-check/bin/python -m
  pytest ...`) — dead weight, safe to prune.
- New, unreviewed: `.github/workflows/claude-code-review.yml` (Claude
  Autonomous Code Review). Uses `gh secret list` permission — confirm no
  secrets are echoed to logs before merging.

## 3. Roadmap (priority order)

**P0 — before any further work**
1. Confirm the concurrent session is idle (check its own terminal/log, not
   just `ps aux`).
2. Run `coverage report` and `python3 scripts/check_coverage_floors.py` from
   *that* session once idle — get one authoritative, current number for
   global % and per-file floors. Do not trust `SESSION_STATE.json` or
   assumptions.
3. Resolve Debt-003: recreate `.venv` pinned to 3.11 (`uv python pin 3.11 &&
   uv sync`), rerun full suite — 3.14 vs 3.11 behavior drift is a silent
   correctness risk (asyncio semantics, deprecations).

**P1 — repo hygiene (cheap, unblocks clean commits)**
4. Delete/gitignore `.claude/settings.json.bak.*` (5 stale files).
5. Prune `.claude/settings.local.json` allow-list back to durable entries;
   drop one-off debug commands tied to now-finished tasks.
6. Decide fate of `src/tuning/live_overrides.py` (untracked) — commit or
   discard; it's currently invisible to git history and CI.

**P2 — commit the in-flight work**
7. Once P0/P1 clear: stage the 17 modified files as scoped commits
   (self-tuning Phase 8 items are already separated by prior commit
   messages — follow that granularity, don't squash into one).
8. Review `.github/workflows/claude-code-review.yml` for secret exposure,
   then merge.

**P3 — architecture decisions (not coverage-blocked, can run in parallel)**
9. GAP-006: TimescaleDB vs QuestDB — needs a written ADR (ingestion volume,
   ops burden, existing SQLite migration path) before implementation starts.
10. Re-run `scripts/check_coverage_floors.py` after any src/execution or
    src/engine change — global 95% does not protect those paths individually.

**P4 — after 95% is *confirmed* (not assumed)**
11. Mutation testing pass on `src/risk/` and `src/execution/live` (highest
    blast radius, per the deny-list itself) — line coverage alone doesn't
    prove these are correct.
12. Security: `bandit` config exists in pyproject — confirm it's wired into
    CI, not just local pre-commit.
13. Staging/paper-trade soak test before any `TRADING_MODE=live` flip (which
    is explicitly denied by current Claude permissions — human-only action).
