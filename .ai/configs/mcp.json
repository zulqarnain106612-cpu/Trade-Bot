{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/fujitsu/Projects/Trade-Bot-main"],
      "description": "Project filesystem access"
    },
    "git": {
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "/home/fujitsu/Projects/Trade-Bot-main"],
      "description": "Git operations"
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "description": "HTTP fetch for market data / docs"
    }
  },
  "notes": "Activate servers as needed. Credentials via .env only — never hard-code."
}
