# Regenerated after the config purge (#144).
#
# Grouped deliberately: the pre-purge config opened one PR per package, which
# is how 11 dependency PRs accumulated against manifests that had been deleted.
version: 2

updates:
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      python-minor-patch:
        applies-to: version-updates
        update-types: ["minor", "patch"]
      python-security:
        applies-to: security-updates
        patterns: ["*"]
    ignore:
      # Major bumps of the heavy ML stack need a deliberate migration, not a bot.
      - dependency-name: torch
        update-types: ["version-update:semver-major"]
      - dependency-name: transformers
        update-types: ["version-update:semver-major"]
      - dependency-name: mlflow
        update-types: ["version-update:semver-major"]

  - package-ecosystem: npm
    directory: "/frontend"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      frontend-build:
        patterns: ["vite", "@vitejs/*", "tailwindcss", "postcss", "autoprefixer"]
      frontend-runtime:
        patterns: ["react", "react-dom", "recharts"]
      electron:
        patterns: ["electron", "electron-builder"]
      # Catch-all, listed last so the named groups above claim their members
      # first. Without it every ungrouped frontend package still opens its own
      # PR -- which is how #141 (es-toolkit) ended up orphaned against a
      # manifest that no longer declared the dependency.
      frontend-other:
        patterns: ["*"]

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 3
    groups:
      actions:
        patterns: ["*"]

  # SUP-005/SUP-006 -- the base image is a dependency like any other, and the
  # one nobody thinks to update: it is pinned by digest (see Dockerfile), so
  # without this entry it would stay on whatever was current the day it was
  # written, accumulating CVEs that the Trivy job would then block a release
  # on. The bot moves the digest; the scan decides whether it was enough.
  - package-ecosystem: docker
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 3
